global:
  scrape_interval: 5s

rule_files:
  - "alerts.yml"

alerting:
  alertmanagers:
    - static_configs:
        - targets: ["host.containers.internal:9093"]

scrape_configs:
  - job_name: "product-service"
    static_configs:
      - targets: ["host.containers.internal:5000"]
